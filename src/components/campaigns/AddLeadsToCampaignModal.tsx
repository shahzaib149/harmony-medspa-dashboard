"use client";
import Papa from "papaparse";
import { FileUp, Search, X } from "lucide-react";
import { useEffect,useMemo,useRef,useState } from "react";
import { BulkActionConfirmDialog } from "@/components/ui/ConfirmDialog";
import type { BulkEnrollmentResult } from "@/lib/types/campaigns";
type Lead={id:string;name:string;email:string;phone:string;status:string;replied:boolean;campaigns?:Array<{campaign:string;status:string}>};
type NewLead={name:string;email:string;phone:string;message:string;source:string;notes:string};
type CsvRow=NewLead&{rowId:string;rowNumber:number;existing?:Lead;reason?:string};
const blank:NewLead={name:"",email:"",phone:"",message:"",source:"Manual Campaign Entry",notes:""};
const blocked:Record<string,string>={Booked:"Booked",Duplicate:"Duplicate",Failed:"Failed Lead","Not Interested":"Not interested"};
const digits=(value:string)=>value.replace(/\D/g,"").replace(/^1(?=\d{10}$)/,"");
function eligibility(lead:Lead){if(blocked[lead.status])return blocked[lead.status];if(lead.replied)return"Replied";if(lead.campaigns?.some(item=>item.campaign==="14-Day Nurture"&&item.status==="Active"))return"Already active";if(!lead.email||digits(lead.phone).length!==10)return"Valid email and US phone required";return""}
function firstSend(date:string,time:string){if(!date||!time)return"";const probe=new Date(`${date}T12:00:00Z`);const zone=new Intl.DateTimeFormat("en-US",{timeZone:"America/New_York",timeZoneName:"longOffset"}).formatToParts(probe).find(part=>part.type==="timeZoneName")?.value.replace("GMT","")||"-04:00";return`${date}T${time}:00${zone}`}
function rowError(row:NewLead){if(!row.name.trim())return"Name is required";if(!row.email.trim()||!row.phone.trim())return"Email and phone are required for nurture";if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(row.email))return"Invalid email";if(digits(row.phone).length!==10)return"Invalid US phone";return""}

export default function AddLeadsToCampaignModal({open,onClose,onComplete}:{open:boolean;onClose:()=>void;onComplete:()=>void}){
 const [tab,setTab]=useState<"existing"|"new"|"csv">("existing"),[leads,setLeads]=useState<Lead[]>([]),[selected,setSelected]=useState<Set<string>>(new Set()),[query,setQuery]=useState(""),[status,setStatus]=useState("All"),[newLead,setNewLead]=useState<NewLead>(blank),[csvRows,setCsvRows]=useState<CsvRow[]>([]),[csvSelected,setCsvSelected]=useState<Set<string>>(new Set()),[fileName,setFileName]=useState(""),[date,setDate]=useState(""),[time,setTime]=useState("16:00"),[permission,setPermission]=useState(false),[saving,setSaving]=useState(false),[confirming,setConfirming]=useState(false),[error,setError]=useState(""),[result,setResult]=useState<BulkEnrollmentResult|null>(null);
 const master=useRef<HTMLInputElement>(null),fileRef=useRef<HTMLInputElement>(null);
 useEffect(()=>{if(!open)return;setSelected(new Set());setCsvSelected(new Set());setPermission(false);setResult(null);setError("");void fetch("/api/airtable/leads?status=all",{cache:"no-store"}).then(response=>response.json()).then(body=>setLeads(body.leads??[]))},[open]);
 const visible=useMemo(()=>leads.filter(lead=>(status==="All"||lead.status===status)&&`${lead.name} ${lead.email} ${lead.phone}`.toLowerCase().includes(query.toLowerCase())),[leads,query,status]);
 const eligibleVisible=visible.filter(lead=>!eligibility(lead));
 useEffect(()=>{if(master.current)master.current.indeterminate=eligibleVisible.some(lead=>selected.has(lead.id))&&!eligibleVisible.every(lead=>selected.has(lead.id))},[eligibleVisible,selected]);
 const count=tab==="existing"?selected.size:tab==="new"?1:csvSelected.size,ready=count>0&&Boolean(date&&time&&permission)&&(tab!=="new"||!rowError(newLead));
 const update=(key:keyof NewLead,value:string)=>setNewLead(current=>({...current,[key]:value}));
 function parseCsv(file:File){setFileName(file.name);Papa.parse<Record<string,string>>(file,{header:true,skipEmptyLines:"greedy",complete:({data,meta})=>{const header=(wanted:string[])=>meta.fields?.find(field=>wanted.includes(field.toLowerCase().replace(/[^a-z]/g,"")))??"";const keys={name:header(["name","fullname","leadname"]),email:header(["email","emailaddress"]),phone:header(["phone","phonenumber","mobile"]),message:header(["message","inquiry"]),source:header(["source","leadsource"]),notes:header(["notes","note"])};const seen=new Set<string>();const rows=data.map((raw,index)=>{const row:CsvRow={rowId:`row-${index+2}`,rowNumber:index+2,name:raw[keys.name]??"",email:raw[keys.email]??"",phone:raw[keys.phone]??"",message:raw[keys.message]??"",source:raw[keys.source]||"Manual Campaign Entry",notes:raw[keys.notes]??""};const existing=leads.find(lead=>row.email&&lead.email.toLowerCase()===row.email.toLowerCase()||row.phone&&digits(lead.phone)===digits(row.phone));const key=row.email.toLowerCase()||digits(row.phone);row.existing=existing;row.reason=rowError(row)||seen.has(key)&&"Duplicate row in file"||existing&&eligibility(existing)||"";if(key)seen.add(key);return row});setCsvRows(rows);setCsvSelected(new Set(rows.filter(row=>!row.reason).map(row=>row.rowId)))},error:()=>setError("The CSV could not be read. Check the file and try again.")})}
 async function enroll(){setSaving(true);setError("");try{const url=tab==="csv"?"/api/airtable/nurture-enrollments/import":"/api/airtable/nurture-enrollments/bulk-enroll";const payload=tab==="csv"?{rows:csvRows,selectedRowIds:[...csvSelected],firstSendAt:firstSend(date,time),timezone:"America/New_York"}:{leadIds:tab==="existing"?[...selected]:[],newLeads:tab==="new"?[newLead]:[],firstSendAt:firstSend(date,time),timezone:"America/New_York"};const response=await fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const body=await response.json();if(!response.ok&&!body.enrolled)throw new Error(body.error||"Leads could not be enrolled");setResult(body);setConfirming(false);onComplete()}catch(event){setError(event instanceof Error?event.message:"Leads could not be enrolled")}finally{setSaving(false)}}
 if(!open)return null;return <div className="fixed inset-0 z-[80] flex bg-black/75 sm:items-center sm:justify-center sm:p-4" role="dialog" aria-modal="true" aria-labelledby="add-leads-title"><div className="flex h-full w-full flex-col border-[#C9A84C]/25 bg-[#101016] sm:max-h-[94vh] sm:max-w-6xl sm:rounded-2xl sm:border"><header className="flex items-start justify-between border-b border-white/10 p-5"><div><h2 id="add-leads-title" className="font-serif text-2xl text-[#F0ECE4]">Add Leads to 14-Day Nurture</h2><p className="mt-1 text-sm text-[#9292A0]">Create enrollment records only. Make.com sends messages when they become due.</p></div><button aria-label="Close" onClick={onClose}><X className="text-[#9292A0]"/></button></header>{result?<Result result={result} onClose={onClose}/>:<><nav className="flex overflow-x-auto border-b border-white/10 px-4">{[["existing","Select Existing Leads"],["new","Add New Lead"],["csv","Import CSV"]].map(([key,label])=><button key={key} onClick={()=>setTab(key as typeof tab)} className="whitespace-nowrap border-b-2 px-4 py-3 text-sm font-bold" style={{color:tab===key?"#C9A84C":"#9292A0",borderColor:tab===key?"#C9A84C":"transparent"}}>{label}</button>)}</nav><div className="flex-1 overflow-y-auto p-5">{tab==="existing"&&<Existing leads={visible} selected={selected} query={query} status={status} master={master} onQuery={setQuery} onStatus={setStatus} onSelected={setSelected}/>} {tab==="new"&&<NewLeadForm value={newLead} update={update} match={leads.find(lead=>newLead.email&&lead.email.toLowerCase()===newLead.email.toLowerCase()||newLead.phone&&digits(lead.phone)===digits(newLead.phone))}/>} {tab==="csv"&&<CsvPanel rows={csvRows} selected={csvSelected} fileName={fileName} inputRef={fileRef} onFile={parseCsv} onClear={()=>{setCsvRows([]);setCsvSelected(new Set());setFileName("")}} onSelected={setCsvSelected}/>}<Schedule date={date} time={time} permission={permission} onDate={setDate} onTime={setTime} onPermission={setPermission}/>{error&&<p role="alert" className="mt-3 text-sm text-[#F58A91]">{error}</p>}</div><footer className="flex items-center justify-between border-t border-white/10 p-5"><p className="text-sm text-[#9292A0]">{count} selected</p><button disabled={!ready||saving} onClick={()=>setConfirming(true)} className="rounded-xl bg-[#C9A84C] px-5 py-2.5 text-sm font-bold text-black disabled:opacity-40">{tab==="csv"?`Start nurture for ${count} Leads`:`Add ${count} Lead${count===1?"":"s"} to Campaign`}</button></footer></>}<BulkActionConfirmDialog open={confirming} title={`Start 14-Day Nurture for ${count} Leads?`} description="The selected Leads will be enrolled in the 14-Day Nurture campaign. Make.com will begin sending messages when the selected First Send At time becomes due." confirmLabel={`Enroll ${count} Leads`} loading={saving} onCancel={()=>setConfirming(false)} onConfirm={()=>void enroll()}><p>First Send At: <b>{formatSchedule(date,time)}</b></p><p>Timezone: America/New_York</p></BulkActionConfirmDialog></div></div>}
function Existing({leads,selected,query,status,master,onQuery,onStatus,onSelected}:{leads:Lead[];selected:Set<string>;query:string;status:string;master:React.RefObject<HTMLInputElement|null>;onQuery:(v:string)=>void;onStatus:(v:string)=>void;onSelected:(v:Set<string>)=>void}){const eligible=leads.filter(lead=>!eligibility(lead));const all=eligible.length>0&&eligible.every(lead=>selected.has(lead.id));return <><div className="flex flex-col gap-2 sm:flex-row"><label className="relative flex-1"><Search className="absolute left-3 top-3 text-[#9292A0]" size={15}/><input value={query} onChange={event=>onQuery(event.target.value)} placeholder="Search Leads" className="w-full rounded-lg border border-white/10 bg-black/20 py-2.5 pl-9 text-white"/></label><select value={status} onChange={event=>onStatus(event.target.value)} className="rounded-lg border border-white/10 bg-[#15151c] px-3 text-white"><option>All</option>{["New","Contacted","Booked","Duplicate","Failed","Not Interested"].map(value=><option key={value}>{value}</option>)}</select><button onClick={()=>onSelected(new Set(eligible.map(lead=>lead.id)))} className="rounded-lg border border-white/10 px-3 text-xs text-white">Select all eligible</button><button onClick={()=>onSelected(new Set())} className="px-3 text-xs text-[#9292A0]">Clear</button></div><div className="mt-4 overflow-x-auto rounded-xl border border-white/10"><table className="w-full min-w-[700px] text-left text-xs"><thead className="bg-[#181820] text-[#9292A0]"><tr><th className="p-3"><input ref={master} type="checkbox" checked={all} aria-label="Select all visible eligible Leads" onChange={event=>onSelected(event.target.checked?new Set(eligible.map(lead=>lead.id)):new Set())}/></th>{["Lead","Contact","Status","Eligibility"].map(value=><th key={value} className="p-3">{value}</th>)}</tr></thead><tbody>{leads.map(lead=>{const why=eligibility(lead);return <tr key={lead.id} className="border-t border-white/5"><td className="p-3"><input type="checkbox" disabled={Boolean(why)} checked={selected.has(lead.id)} aria-label={`Select ${lead.name}`} onChange={event=>{const next=new Set(selected);if(event.target.checked)next.add(lead.id);else next.delete(lead.id);onSelected(next)}}/></td><td className="p-3 font-bold text-white">{lead.name}</td><td className="p-3 text-[#B8B8C2]">{lead.email||lead.phone}</td><td className="p-3 text-[#B8B8C2]">{lead.status}</td><td className={why?"p-3 text-[#E6AD55]":"p-3 text-[#4ECDC4]"}>{why||"Eligible"}</td></tr>})}</tbody></table></div></>}
function NewLeadForm({value,update,match}:{value:NewLead;update:(key:keyof NewLead,value:string)=>void;match?:Lead}){return <div className="mx-auto grid max-w-3xl gap-4 sm:grid-cols-2">{(["name","email","phone","source","message","notes"] as const).map(key=><label key={key} className={key==="message"||key==="notes"?"sm:col-span-2":""}><span className="mb-1 block text-xs font-bold capitalize text-[#9292A0]">{key}{key==="name"?" *":""}</span><input value={value[key]} onChange={event=>update(key,event.target.value)} className="h-11 w-full rounded-xl border border-white/10 bg-black/20 px-3 text-white"/></label>)}{match&&<p className="sm:col-span-2 rounded-xl border border-[#4ECDC4]/20 bg-[#4ECDC4]/5 p-3 text-sm text-[#4ECDC4]">Existing Lead found: {match.name}. This record will be used if eligible.</p>}{rowError(value)&&<p className="sm:col-span-2 text-sm text-[#E6AD55]">{rowError(value)}</p>}</div>}
function CsvPanel({rows,selected,fileName,inputRef,onFile,onClear,onSelected}:{rows:CsvRow[];selected:Set<string>;fileName:string;inputRef:React.RefObject<HTMLInputElement|null>;onFile:(f:File)=>void;onClear:()=>void;onSelected:(v:Set<string>)=>void}){if(!rows.length)return <div onDragOver={event=>event.preventDefault()} onDrop={event=>{event.preventDefault();const file=event.dataTransfer.files[0];if(file?.name.endsWith(".csv"))onFile(file)}} className="rounded-2xl border border-dashed border-[#C9A84C]/30 bg-[#C9A84C]/[.03] p-12 text-center"><FileUp className="mx-auto text-[#C9A84C]"/><h3 className="mt-4 font-serif text-xl text-white">Upload Lead CSV</h3><p className="mt-2 text-sm text-[#9292A0]">Headers can include Name, Email, Phone, Message, Source, and Notes. Quoted commas and line breaks are supported.</p><input ref={inputRef} hidden type="file" accept=".csv,text/csv" onChange={event=>{const file=event.target.files?.[0];if(file)onFile(file)}}/><button onClick={()=>inputRef.current?.click()} className="mt-5 rounded-xl bg-[#C9A84C] px-4 py-2 text-sm font-bold text-black">Browse file</button></div>;return <><div className="flex items-center justify-between"><p className="text-sm text-white">{fileName} · {rows.length} rows</p><div className="flex gap-3"><button onClick={()=>onSelected(new Set(rows.filter(row=>!row.reason).map(row=>row.rowId)))} className="text-xs font-bold text-[#C9A84C]">Select all eligible</button><button onClick={()=>onSelected(new Set())} className="text-xs text-[#9292A0]">Clear</button><button onClick={onClear} className="text-xs text-[#F58A91]">Remove file</button></div></div><div className="mt-4 max-h-80 overflow-auto rounded-xl border border-white/10"><table className="w-full min-w-[760px] text-left text-xs"><thead className="sticky top-0 bg-[#181820] text-[#9292A0]"><tr>{["","Row","Lead","Email","Phone","Existing / New","Validation"].map(value=><th key={value} className="p-3">{value}</th>)}</tr></thead><tbody>{rows.map(row=><tr key={row.rowId} className="border-t border-white/5"><td className="p-3"><input type="checkbox" disabled={Boolean(row.reason)} checked={selected.has(row.rowId)} onChange={event=>{const next=new Set(selected);if(event.target.checked)next.add(row.rowId);else next.delete(row.rowId);onSelected(next)}}/></td><td className="p-3 text-[#9292A0]">{row.rowNumber}</td><td className="p-3 text-white">{row.name}</td><td className="p-3 text-[#B8B8C2]">{row.email}</td><td className="p-3 text-[#B8B8C2]">{row.phone}</td><td className="p-3"><span className="rounded-full bg-white/5 px-2 py-1 text-[#4ECDC4]">{row.existing?"Existing":"New"}</span></td><td className={row.reason?"p-3 text-[#E6AD55]":"p-3 text-[#4ECDC4]"}>{row.reason||"Eligible"}</td></tr>)}</tbody></table></div></>}
function Schedule({date,time,permission,onDate,onTime,onPermission}:{date:string;time:string;permission:boolean;onDate:(v:string)=>void;onTime:(v:string)=>void;onPermission:(v:boolean)=>void}){return <div className="mt-5 grid gap-4 rounded-2xl border border-white/10 p-4 sm:grid-cols-4"><div><p className="text-[10px] uppercase text-[#9292A0]">Campaign</p><p className="text-sm text-white">14-Day Nurture</p></div><label><span className="text-[10px] uppercase text-[#9292A0]">First Send Date</span><input type="date" required value={date} onChange={event=>onDate(event.target.value)} className="mt-1 w-full rounded-lg bg-black/20 p-2 text-white"/></label><label><span className="text-[10px] uppercase text-[#9292A0]">First Send Time</span><input type="time" required value={time} onChange={event=>onTime(event.target.value)} className="mt-1 w-full rounded-lg bg-black/20 p-2 text-white"/></label><div><p className="text-[10px] uppercase text-[#9292A0]">Timezone</p><p className="text-sm text-white">America/New_York</p></div><label className="flex items-center gap-3 sm:col-span-4 text-sm text-[#B8B8C2]"><input type="checkbox" checked={permission} onChange={event=>onPermission(event.target.checked)}/>SMS permission has been verified for all selected Leads</label></div>}
function Result({result,onClose}:{result:BulkEnrollmentResult;onClose:()=>void}){return <div className="overflow-y-auto p-6"><h3 className="font-serif text-2xl text-[#4ECDC4]">Enrollment results</h3><div className="mt-4 grid gap-3 sm:grid-cols-3"><p className="rounded-xl bg-[#4ECDC4]/10 p-4 text-[#4ECDC4]">{result.enrolled.length} enrolled</p><p className="rounded-xl bg-amber-500/10 p-4 text-amber-300">{result.skipped.length} skipped</p><p className="rounded-xl bg-red-500/10 p-4 text-red-300">{result.failed.length} failed</p></div>{[...result.skipped,...result.failed].map((item,index)=><p key={index} className="mt-2 text-sm text-[#B8B8C2]">{item.name||item.leadId}: {item.reason}</p>)}<button onClick={onClose} className="mt-6 rounded-xl bg-[#C9A84C] px-5 py-2 font-bold text-black">Done</button></div>}
function formatSchedule(date:string,time:string){return date&&time?new Date(firstSend(date,time)).toLocaleString("en-US",{dateStyle:"medium",timeStyle:"short"}):"Not selected"}
